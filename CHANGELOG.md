# Changelog

## 2.5.0
支持重发

## 2.4.0
添加顶部说明

## 2.3.0

增加stay功能

## 2.2.1 - 2026-04-29

Tag name支持任意内容

## 2.2.0 — 2026-04-28

### Added

- **冷却期**（`cooldown`）：条目级别新增 `cooldown` 字段（整数，单位：轮）。条目注入后，接下来 N 轮内即使正则再次命中也跳过注入，第 N+1 轮若触发则重新注入并重置冷却。冷却按会话独立追踪，通过 `constant` 或 `links` 被拉入的条目同样受自身冷却约束。默认为 `0`（不冷却，与之前行为一致）。

## 2.1.0 — 2026-04-21

### Added

- **常驻条目**（`constant`）：条目级别新增 `constant` 字段。设为 `true` 后，当同一世界书中任意其他条目被正则命中时，该条目自动追加注入。适用于需要始终提供背景上下文的总纲条目。
- **条目链接**（`links`）：条目级别新增 `links` 字段，接受条目名数组或逗号分隔字符串。当此条目被命中时，追加注入指定的条目。链接为单层解析，不递归传播；双向链接通过互相填写对方名称实现。
- **编辑器 UI 支持**：`lorebook_helper.html` 编辑器中每个条目新增常驻开关和链接输入框。

## 2.0.0 — 2026-04-21

### Changed

- **配置方式重构**：废弃 `template_list` 多层嵌套表单，改为单个文本框直接粘贴 JSON。管理面板只有一个字段 `lorebooks_json`。
- **字段重命名**：`keywords` → `regex`，更准确地反映字段语义。
- **条目数量不再限制**：移除 20 个条目的硬上限，每本世界书的条目数量仅受 JSON 大小限制。
- **灵活的输入格式**：`enabled` 接受 `"T"`/`"F"` 或 `true`/`false`；`priority` 接受字符串或数字；`regex` 接受 `|` 分隔的字符串或数组；`entries` 接受对象或数组。
- **配置格式不兼容**：旧版 `template_list` 配置不再兼容，升级后需要在文本框中重新粘贴 JSON。
- **版本号升至 2.0.0**。

## 1.2.0 — 2026-04-20

### Added

- **多世界书架构**：顶层配置从扁平条目列表重构为 `template_list` 多世界书结构。每本世界书包含独立的启用开关、注入位置、XML 标签名称、标签头部说明文本，以及最多 20 个条目槽位。展开一本世界书即可查看和编辑所有条目，无需逐个点开。
- **标签头部说明文本**（`header_text`）：可为每本世界书配置一段注入内容开头的说明文字，紧跟在 XML 开标签之后、条目内容之前。留空则不添加。

### Changed

- **版本号升至 1.2.0**。
- **配置格式不兼容**：旧版扁平 `entries` 配置不再兼容，升级后需要在管理面板重新配置条目。

## 1.1.0 — 2026-04-13

### Added

- **RAG 去重**：注入阶段新增去重逻辑。当世界书条目内容已被 RAG（LivingMemory 向量检索）注入到 `req.prompt` 中时，自动跳过该条目，避免同一段信息重复出现在上下文中。比较前会对文本做空白归一化（连续空白压缩为单个空格），容忍轻微的格式差异。跳过的条目会记录到日志中。

### Changed

- **单条目不加序号**：当仅匹配到一个条目时，注入内容不再添加 `#1` 序号前缀，减少不必要的格式噪音。

## 1.0.0 — 2026-04-07

- 初始版本：基于正则关键词匹配的轻量世界书实现
