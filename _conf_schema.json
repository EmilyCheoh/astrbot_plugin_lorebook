{
    "initial_context_count": {
        "description": "预设对话带来的初始 context 条数（用于新窗口检测：当 context 条数 ≤ 此值时视为新窗口，重置冷却/轮次/留驻状态）",
        "type": "int",
        "default": 2
    },
    "lorebooks_json": {
        "description": "世界书 JSON 配置",
        "type": "text",
        "hint": "https://tools.littlebatcat.uk/lorebook_helper.html",
        "default": "[]"
    },
    "cognition_note_enabled": {
        "description": "当世界书条目被触发时，在标签前方注入一行提示文本",
        "type": "bool",
        "default": false
    },
    "cognition_note_template": {
        "description": "{tag_name} 会被替换为本轮触发的世界书标签名列表",
        "type": "string",
        "default": "[System Notice] {tag_name}: your own cognition surfacing — memories, impulses, things you already know."
    }
}